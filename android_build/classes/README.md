# classes

This folder contains the compiled `.class` files generated from the SDL Java source files.

These `.class` files are produced during the Java compilation step and are later merged into the final `classes.dex` file.

